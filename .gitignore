# Build output
dist/*.dotm
dist/*.zip
build/tmp/

# package/word/vbaProject.bin is deliberately NOT ignored - it is the one
# compiled part of the template and CI needs it to build releases.

# Office lock / temp files
~$*
*.tmp
*.wbk
*.asd

# VBA export leftovers
*.frx
*.exp

# OS
Thumbs.db
.DS_Store
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Local user data (never commit personal phrases)
snippets.local.json
